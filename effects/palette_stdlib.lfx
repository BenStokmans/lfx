version "0.1"
module "effects/palette_stdlib"
import "std/palette" as palette
effect "Palette Stdlib"
output rgb

function sample(width, height, x, y, index, phase, params)
  t = x / max(width, 1.0) + phase
  return palette.rainbow(fract(t))
end

timeline {
  loop_start = 0.0
  loop_end = 1.0
}
