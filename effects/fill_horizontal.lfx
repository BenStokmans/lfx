module "effects/fill_horizontal"
effect "fill_horizontal"
output scalar

function sample(width, height, x, y, index, phase, params)
  span = max(width - 1.0, 1.0)
  return clamp(x / span, 0.0, 1.0)
end
